import React from "react";

const VideoThumbnail = () => {
  return (
    <div className="flex items-center justify-center rounded-md bg-gradient-to-br from-layerPink-80 to-layerPurple-80 backdrop-blur-[50px] w-full aspect-video">
      VideoThumbnail
    </div>
  );
};

export default VideoThumbnail;
